Паттерн Адаптер используется, когда есть старый класс с несовместимым интерфейсом (Adaptee), но клиентский код ожидает другой интерфейс (Target). создается класс-Adapter, который реализует целевой интерфейс, внутри содержит объект Adaptee и преобразует вызовы из одного формата в другой. В примере: клиент (NotificationService) хочет отправлять сообщения через единый интерфейс IMessageSender с методом Send(header, body). Но есть старые сервисы: LegacyEmailService.SendEmail(fullMessage) и LegacySmsService.SendSms(phone, text). создается EmailAdapter и SmsAdapter, которые реализуют IMessageSender, внутри вызывают методы старых сервисов, преобразуя параметры (header+body склеиваются в письмо или header используется как номер телефона). В итоге клиент ничего не знает о старых классах и работает только через удобный ему интерфейс.
